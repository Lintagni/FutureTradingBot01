app = "futures-trading-bot-wmg"
primary_region = "fra"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"
  PORT = "8080"

[[mounts]]
  source = "trading_data"
  destination = "/data"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

  [http_service.http_options]
    h2_backend = false  # WebSocket upgrades require HTTP/1.1, not HTTP/2

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
